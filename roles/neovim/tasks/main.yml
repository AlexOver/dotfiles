# file: roles/neovim/tasks/main.yml
- name: Install neovim dependancies
  become: true
  ansible.builtin.package:
    name:
      - npm
      - gettext
    state: present
  when: ansible_facts['distribution'] == 'Archlinux'

- name: Install Neovim
  block:
    - name: Clone/Update Neovim
      ansible.builtin.git:
        repo: "https://github.com/neovim/neovim.git"
        dest: '{{ lookup("env", "HOME") }}/.neovim'
        depth: 1
        version: "stable"
        force: yes
      register: nvim_repo

    - name: Clean stale Neovim build artifacts before rebuilding
      ansible.builtin.command:
        argv:
          - "make"
          - "distclean"
        chdir: '{{ lookup("env", "HOME") }}/.neovim'
      when: nvim_repo.changed

    - name: Build Neovim
      ansible.builtin.command:
        argv:
          - "make"
          - "-j4"
          - "CMAKE_BUILD_TYPE=RelWithDebInfo"
          - 'CMAKE_INSTALL_PREFIX={{ lookup("env", "HOME") }}/.local'
        chdir: '{{ lookup("env", "HOME") }}/.neovim'
      when: nvim_repo.changed

    - name: Run make install for Neovim
      ansible.builtin.command:
        argv:
          - "make"
          - "install"
        chdir: '{{ lookup("env", "HOME") }}/.neovim'
      when: nvim_repo.changed

- name: Create .config directory if missing | file
  ansible.builtin.file:
    path: '{{ lookup("env", "HOME") }}/.config'
    state: directory
    mode: "0755"

- name: Check for nvim folder | stat
  ansible.builtin.stat:
    path: "{{ lookup('env', 'HOME') }}/.config/nvim"
  register: nvim_stat

- name: Clone LazyVim
  ansible.builtin.git:
    repo: "https://github.com/LazyVim/starter"
    depth: 1
    dest: '{{ lookup("env", "HOME") }}/.config/nvim'
  when: not nvim_stat.stat.exists

- name: Copy multiple config files (only if not already present)
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/neovim/files/{{ item.src }}"
    dest: "{{ lookup('env', 'HOME') }}/.config/nvim/{{ item.dest }}"
    mode: "0644"
    force: false
  loop:
    - { src: "telescope.lua", dest: "lua/plugins/telescope.lua" }
    - { src: "lazyvim.json", dest: "lazyvim.json" }
    - { src: "keymaps.lua", dest: "lua/config/keymaps.lua" }

- name: Ensure lazygit config directory exists
  ansible.builtin.file:
    path: '{{ lookup("env", "HOME") }}/.config/lazygit'
    state: directory
    mode: "0755"

- name: Copy lazygit config (only if not already present)
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/roles/neovim/files/lazygit_config.yml"
    dest: '{{ lookup("env", "HOME") }}/.config/lazygit/config.yml'
    mode: "0644"
    force: false

- name: Attempt to download Lazygit
  block:
    - name: Determine latest Github release of Lazygit
      ansible.builtin.uri:
        url: "https://api.github.com/repos/jesseduffield/lazygit/releases/latest"
        body_format: json
      register: _github_release
      until: _github_release.status == 200
      retries: 5

    - name: Set lazygit_version
      ansible.builtin.set_fact:
        lazygit_version: '{{ _github_release.json.tag_name | regex_replace("^v", "") }}'

    - name: Check if Lazygit is installed
      ansible.builtin.stat:
        path: '{{ lookup("env", "HOME") }}/.local/bin/lazygit'
      register: lazygit_stat

    - name: Determine installed version of Lazygit
      ansible.builtin.command: lazygit --version
      register: lazygit_installed_version_raw
      when: lazygit_stat.stat.exists
      changed_when: false

    - name: Extract installed version of Lazygit
      ansible.builtin.set_fact:
        lazygit_installed_version: "{{ lazygit_installed_version_raw.stdout | regex_search('version=([0-9.]+)', '\\1') | first }}"
      when: lazygit_installed_version_raw is defined

    - name: Show installed version of Lazygit
      ansible.builtin.debug:
        msg: "Installed version: {{ lazygit_installed_version }}"
      when: lazygit_installed_version is defined

    - name: Show latest version of Lazygit
      ansible.builtin.debug:
        msg: "Latest version: {{ lazygit_version }}"
      when: lazygit_version is defined

    - name: Determine Lazygit release architecture
      ansible.builtin.set_fact:
        lazygit_arch: "{{ {'x86_64': 'x86_64', 'aarch64': 'arm64', 'armv7l': 'armv7', 'armv6l': 'armv6', 'i686': '386'}[ansible_facts['architecture']] }}"

    - name: Download Lazygit
      ansible.builtin.unarchive:
        src: "https://github.com/jesseduffield/lazygit/releases/download/v{{ lazygit_version }}/lazygit_{{ lazygit_version }}_Linux_{{ lazygit_arch }}.tar.gz"
        dest: '{{ lookup("env", "HOME") }}/.local/bin'
        include: "lazygit"
        mode: "0755"
        remote_src: true
      when: (lazygit_installed_version is not defined) or (lazygit_installed_version != lazygit_version)
